worker_processes  2;
daemon on;
master_process off;
error_log /usr/home/yaoweibin/work/public/nginx_secure_cookie_module/test/t/servroot/logs/error.log debug;
pid       /usr/home/yaoweibin/work/public/nginx_secure_cookie_module/test/t/servroot/logs/nginx.pid;

http {
# Begin test case config...
    server {
    listen       1982;
    server_name  localhost;

    location  /{
        secure_cookie $cookie_CAPTCHA_SESSION,$cookie_CAPTCHA_EXPIRES;
        secure_cookie_md5 private_key$binary_remote_addr$cookie_CAPTCHA_EXPIRES;

        if ($secure_cookie = "") {
            return 403;
        }

        if ($secure_cookie = "0") {
            return 404;
        }

        root   html;
        index  index.html index.htm;
    }
}

# End test case config.
}

events {
    worker_connections  1024;
}

